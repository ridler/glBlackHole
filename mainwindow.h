#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include "helpers.h"
#include "viewport.h"

class MainWindow : public QWidget
{
Q_OBJECT
private:

public:
   MainWindow(QWidget* parent=0);
private slots:
   // maybe
};

#endif // MAINWINDOW_H
